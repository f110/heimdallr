# permissions to do viewer roles.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: role-viewer-role
rules:
- apiGroups:
  - proxy.f110.dev
  resources:
  - roles
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - proxy.f110.dev
  resources:
  - roles/status
  verbs:
  - get
