# permissions to do viewer backends.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: backend-viewer-role
rules:
- apiGroups:
  - proxy.f110.dev
  resources:
  - backends
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - proxy.f110.dev
  resources:
  - backends/status
  verbs:
  - get
