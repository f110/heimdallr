- name: admin
  title: Administrator
  description: for administrator
  bindings:
    - backend: test.local-proxy.f110.dev
      permission: all
    - backend: dashboard.local-proxy.f110.dev
      permission: all
    - backend: ssh.local-proxy.f110.dev
      permission: all
- name: user
  title: User
  bindings:
    - backend: test.local-proxy.f110.dev
      permission: all