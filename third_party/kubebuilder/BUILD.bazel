load("//build/rules/go:vendor.bzl", "go_vendor")

# gazelle:prefix sigs.k8s.io/kubebuilder

go_vendor(name = "vendor")

alias(
    name = "kubebuilder",
    actual = "//third_party/kubebuilder/cmd:cmd",
)
